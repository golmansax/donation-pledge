@require '../../styles/fonts'

$headingScale = 1.5
$baseSize = 1em
$large = $baseSize * $headingScale
$xlarge = $large * $headingScale

.rootElement {
  line-height 1.2
}

.mainHeader {
  composes rootElement
  font-family $headingFontFamily
  font-weight 700
  text-transform uppercase
}

.secondaryHeader {
  composes rootElement
  font-weight 700
}

.h1 {
  composes mainHeader
  font-size $xlarge
}

.h2 {
  composes mainHeader
  font-size $large
}

.h3 {
  composes mainHeader
  font-size $baseSize
}

.h4 {
  composes secondaryHeader
  font-size $baseSize
}
