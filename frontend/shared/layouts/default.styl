@require '_colors'
@require '_fonts'

body {
  background-color $superLightGray
  font-family $textFontFamily
  color $darkGray
  line-height 1.5
}

a {
  color $blue
  transition color 300ms ease-out

  &:hover {
    color $superDarkBlue
  }
}
