@require _colors
@require _fonts
@require _units

.rootElement {
  color $gray
}

.secondary {
  $secondaryLambda = .9em

  font-family $serifFontFamily
  font-size $secondaryLambda
  line-height ($lambda / $secondaryLambda)
}
